# 待实现功能清单

> 2026-07-17: 从 dead code 消除工程中整理出的待实现功能.
> 这些功能原本以 `#[allow(dead_code)]` 预留, 在消除死代码时被删除.
> 本文档记录这些功能, 供后续实现参考.

---

## 一、中断与多核通信

### 1.1 APIC 中断投递模式配置

**描述**: 支持配置 LVT LINT0/LINT1 的投递模式 (Fixed/SMI/NMI/ExtINT)

**当前状态**: 常量已定义但未使用 (已删除)

**待实现功能**:
- `configure_lint0(mode)` - 配置 LINT0 投递模式
- `configure_lint1(mode)` - 配置 LINT1 投递模式
- `delivery_fixed()` - 获取 Fixed 模式常量
- `delivery_smi()` - 获取 SMI 模式常量
- `delivery_nmi()` - 获取 NMI 模式常量
- `delivery_extint()` - 获取 ExtINT 模式常量

**依赖**: 多核中断路由功能

**优先级**: P2 (中)

---

### 1.2 ICI 广播与电平触发

**描述**: 支持发送带 broadcast/level 触发模式的 IPI

**当前状态**: 常量已定义但未使用 (已删除)

**待实现功能**:
- `send_ipi_level(apic_id, vector)` - 发送 level 触发 IPI
- `broadcast_ipi_level(vector)` - 发送 broadcast IPI
- `icr_level()` - 获取 ICR_LEVEL 常量
- `icr_broadcast()` - 获取 ICR_BROADCAST 常量

**依赖**: 多核通信功能

**优先级**: P2 (中)

---

## 二、存储驱动

### 2.1 NVMe 中断管理

**描述**: 支持 NVMe 控制器中断掩码配置

**当前状态**: 寄存器定义已删除

**待实现功能**:
- `NVME_REG_INTMS` - 中断掩码设置寄存器 (0x0C)
- `NVME_REG_INTMC` - 中断掩码清除寄存器 (0x10)
- `enable_interrupt(vector)` - 使能指定中断
- `disable_interrupt(vector)` - 禁用指定中断
- `mask_all_interrupts()` - 屏蔽所有中断
- `unmask_all_interrupts()` - 取消屏蔽所有中断

**依赖**: NVMe 驱动中断处理

**优先级**: P2 (中)

---

## 三、PCI 总线

### 3.1 PCI 低级端口 I/O

**描述**: 支持 PCI 配置空间的 8 位和 16 位端口 I/O 访问

**当前状态**: 函数定义已删除

**待实现功能**:
- `outb(port, val)` - 8 位端口写
- `outw(port, val)` - 16 位端口写
- `inb(port)` - 8 位端口读
- `inw(port)` - 16 位端口读

**使用场景**:
- PCI 配置空间低级访问
- 某些 PCI 设备的特殊寄存器访问

**依赖**: 无

**优先级**: P3 (低)

---

## 四、输入设备

### 4.1 PS/2 键盘控制命令

**描述**: 支持向 PS/2 控制器发送命令, 实现键盘自检和重置

**当前状态**: 函数定义已删除

**待实现功能**:
- `ps2_send_command(cmd)` - 向 PS/2 控制器发送命令
- `ps2_self_test()` - PS/2 控制器自检
- `keyboard_reset()` - 键盘重置

**使用场景**:
- 键盘初始化时自检
- 键盘故障时重置
- 诊断功能

**依赖**: PS/2 驱动

**优先级**: P3 (低)

---

## 五、USB 子系统

### 5.1 USB 端点错误恢复

**描述**: 支持 USB 端点错误恢复, 包括停止、重置端点

**当前状态**: 函数定义已删除

**待实现功能**:
- `recover_endpoint(slot_id, ep_id)` - 端点错误恢复
- `stop_endpoint(slot_id, ep_id)` - 停止指定端点
- `reset_endpoint(slot_id, ep_id)` - 重置端点状态

**使用场景**:
- USB 传输错误恢复
- 端点超时处理
- 设备热插拔后重新配置

**依赖**: xHCI 驱动

**优先级**: P2 (中)

---

## 六、进程管理

### 6.1 页表切换

**描述**: 支持进程页表切换

**当前状态**: FFI 函数声明已删除

**待实现功能**:
- `vmm_switch_page_table(table)` - 切换到指定页表

**使用场景**:
- 进程上下文切换时切换页表
- 内核态/用户态切换

**依赖**: VMM 子系统

**优先级**: P1 (高)

---

### 6.2 进程状态查询

**描述**: 支持查询和检查进程状态

**当前状态**: 方法定义已删除

**待实现功能**:
- `load_state()` - 加载进程状态
- `get_state()` - 获取进程状态
- `is_running()` - 检查进程是否在运行
- `is_exited()` - 检查进程是否已退出

**使用场景**:
- 调度器决策
- 进程管理诊断
- wait4/waitpid 实现

**依赖**: 进程管理子系统

**优先级**: P1 (高)

---

## 七、文件系统

### 7.1 目录缓存管理

**描述**: 支持目录缓存的失效和引用计数管理

**当前状态**: 方法定义已删除

**待实现功能**:
- `invalidate_entry(parent_ino, name)` - 失效指定条目
- `invalidate_all(parent_ino)` - 批量失效父目录条目
- `ref_dec(ino)` - 减少引用计数
- `is_ref_zero(ino)` - 检查引用计数是否为零
- `get_ref_count(ino)` - 获取引用计数

**使用场景**:
- 文件/目录创建/删除/重命名后失效缓存
- 缓存条目生命周期管理
- 内存压力时回收缓存

**依赖**: VFS 子系统

**优先级**: P1 (高)

---

### 7.2 inotify 文件描述符

**描述**: 支持 inotify 实例的文件描述符管理

**当前状态**: 方法定义已删除

**待实现功能**:
- `fd()` - 获取 inotify 实例的 fd
- `is_fd_valid()` - 检查 fd 是否有效

**使用场景**:
- inotify_init/inotify_add_watch 实现
- 文件系统事件通知

**依赖**: inotify 子系统

**优先级**: P2 (中)

---

## 八、网络栈

### 8.1 Unix 域套接字管理

**描述**: 支持 Unix 域套接字的分配和状态查询

**当前状态**: 方法定义已删除

**待实现功能**:
- `find_free_socket()` - 查找空闲套接字槽位
- `has_free_socket()` - 检查是否有空闲槽位
- `used_socket_count()` - 获取已使用套接字数量

**使用场景**:
- socket/bind 实现
- 套接字资源管理
- 诊断和统计

**依赖**: Unix 域套接字子系统

**优先级**: P2 (中)

---

## 九、实施建议

### 9.1 优先级排序

| 优先级 | 功能 | 工作量 | 说明 |
|--------|------|--------|------|
| P1 | 页表切换 | 1 天 | 进程管理核心功能 |
| P1 | 进程状态查询 | 0.5 天 | 调度器核心功能 |
| P1 | 目录缓存管理 | 1 天 | 文件系统核心功能 |
| P2 | APIC 中断投递模式 | 1 天 | 多核中断路由 |
| P2 | ICI 广播与电平触发 | 1 天 | 多核通信 |
| P2 | NVMe 中断管理 | 1 天 | 存储驱动 |
| P2 | USB 端点错误恢复 | 2 天 | USB 驱动 |
| P2 | inotify 文件描述符 | 0.5 天 | 文件系统事件 |
| P2 | Unix 域套接字管理 | 0.5 天 | 网络栈 |
| P3 | PCI 低级端口 I/O | 0.5 天 | PCI 总线 |
| P3 | PS/2 键盘控制命令 | 0.5 天 | 输入设备 |

**总预估**: 10-11 天

### 9.2 实施顺序

1. **Phase 1**: 进程管理 (页表切换 + 状态查询)
2. **Phase 2**: 文件系统 (目录缓存管理)
3. **Phase 3**: 中断与多核 (APIC + ICI)
4. **Phase 4**: 驱动 (NVMe + USB)
5. **Phase 5**: 网络与输入 (Unix + inotify + PS/2 + PCI)

### 9.3 验证标准

每个功能实现后:
1. 双架构编译 0 warning 0 error
2. 相关功能可用 (通过测试验证)
3. 无新增 dead code 警告
4. host-tests 通过

---

**文档状态**: [X] 已创建
